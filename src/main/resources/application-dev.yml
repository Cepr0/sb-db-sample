logging:
  level:
    com.example: debug
    org:
      springframework:
        orm.jpa: debug
        web: debug
    jdbc:
      sqltiming: info
      resultsettable: info

spring:
  flyway:
    clean-on-validation-error: true
